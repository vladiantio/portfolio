---
import type { HTMLAttributes } from "astro/types";

type Props = Omit<HTMLAttributes<"a">, "rel" | "target">;

const props = Astro.props;
---

<a rel="noopener noreferrer" target="_blank" {...props}><slot /></a>
