---
import Layout from "@/layouts/Layout.astro";
import { getLocaleParams, useTranslations } from "@/i18n/helpers";
import type { Locale } from "@/i18n/constants";
import UnderConstruction from "@/sections/UnderConstruction.astro";

const locale = Astro.currentLocale as Locale | undefined;
const t = useTranslations(locale);

export const getStaticPaths = getLocaleParams;
---

<Layout title={t("aboutMe")}>
  <!-- <AboutMeSection />
  <Projects />
  <Timeline /> -->
  <UnderConstruction />
</Layout>
